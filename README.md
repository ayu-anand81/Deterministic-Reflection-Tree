# Deterministic-Reflection-Tree
A deterministic end-of-day reflection tool built using HTML, CSS, JavaScript, and JSON. It guides users through a fixed decision tree across agency, contribution, and perspective axes. No free text or AI-generated runtime output—every path is predefined for consistent and predictable reflection flow.
